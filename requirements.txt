# Core parsing
extract-msg>=0.48        # .msg parsing
# IOC extraction
iocextract>=1.16
tldextract>=5.1
# Attachment static analysis
oletools>=0.60           # olevba / oleid for Office macros
# HTTP for enrichment
httpx>=0.27
# CLI uses the stdlib argparse (no third-party dependency)
# Config
python-dotenv>=1.0
# Web demo UI (Streamlit Community Cloud)
streamlit>=1.36
# Tests
pytest>=8.0
