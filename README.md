# phishing-email-analyzer
A command-line tool that analyzes suspicious emails like a SOC analyst. It validates authentication headers, traces sender origins, extracts and defangs IOCs, inspects attachments, calculates a risk score, and generates both Markdown and JSON reports for investigation and automation workflows.
